<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Lab App 2FA</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding-top: 50px;
        }
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
        }
        .btn-primary {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <%= success %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body p-5">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h2>Dashboard Principal</h2>
                            <form action="/logout" method="POST" class="d-inline">
                                <button type="submit" class="btn btn-outline-danger">Cerrar Sesión</button>
                            </form>
                        </div>
                        
                        <div class="alert alert-success">
                            <h4>¡Bienvenido, <%= user.username %>!</h4>
                            <p class="mb-0">Has iniciado sesión exitosamente con autenticación de 2 factores.</p>
                        </div>

                        <div class="row mt-4">
                            <div class="col-md-6">
                                <div class="card bg-light">
                                    <div class="card-body">
                                        <h5>Información de Usuario</h5>
                                        <p><strong>Usuario:</strong> <%= user.username %></p>
                                        <p><strong>Email:</strong> <%= user.email %></p>
                                        <p><strong>2FA Activado:</strong> 
                                            <span class="badge bg-<%= user.isTwoFactorEnabled ? 'success' : 'warning' %>">
                                                <%= user.isTwoFactorEnabled ? 'Sí' : 'No' %>
                                            </span>
                                        </p>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="col-md-6">
                                <div class="card bg-light">
                                    <div class="card-body">
                                        <h5>Acciones</h5>
                                        <a href="/profile" class="btn btn-primary w-100 mb-2">Mi Perfil</a>
                                        <% if (user.isTwoFactorEnabled) { %>
                                            <form action="/disable-2fa" method="POST" class="d-inline w-100">
                                                <button type="submit" class="btn btn-warning w-100">Desactivar 2FA</button>
                                            </form>
                                        <% } else { %>
                                            <a href="/setup-2fa" class="btn btn-success w-100">Activar 2FA</a>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>